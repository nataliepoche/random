def sum(a, b):
    return a + b

if __name__ == "__main__": # Used so that that you can import definition to another place, used to figure out which file is used for execution
    print(sum(5,6))

# Project 2-B
def to_hex_string(data): # Transfers data into hexidecimal string

def count_runs(flat_data): # Counts the number of runs in an image data set

def encode_rle(rle_data):